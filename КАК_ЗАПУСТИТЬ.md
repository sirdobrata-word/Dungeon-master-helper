# 🚀 Как запустить сервис

## ✅ Правильный способ

### Вариант 1: Через скрипт (рекомендуется)

1. **Открой терминал в папке проекта**
   - Правый клик на папке проекта → "Открыть в терминале"
   - Или открой терминал и перейди в папку:
     ```bash
     cd "C:\Users\ALFA\OneDrive\Рабочий стол\тишина"
     ```

2. **Запусти скрипт:**
   ```bash
   # Windows CMD
   start-server.bat
   
   # Windows PowerShell
   .\start-server.ps1
   ```

### Вариант 2: Напрямую через Go

1. **Открой терминал в папке проекта**

2. **Запусти:**
   ```bash
   go run cmd/server/main.go
   ```

---

## ❌ Частые ошибки

### Ошибка: "The system cannot find the path specified"

**Причина:** Терминал открыт не в правильной директории

**Решение:**
1. Убедись, что терминал открыт в папке проекта
2. Проверь текущую директорию:
   ```bash
   # CMD
   cd
   
   # PowerShell
   pwd
   ```
3. Должно быть: `C:\Users\ALFA\OneDrive\Рабочий стол\тишина`

4. Если нет - перейди в правильную директорию:
   ```bash
   cd "C:\Users\ALFA\OneDrive\Рабочий стол\тишина"
   ```

---

### Ошибка: "go: cannot find main module"

**Причина:** Терминал не в корне проекта или нет go.mod

**Решение:**
1. Проверь, что файл `go.mod` существует в текущей директории
2. Перейди в корень проекта:
   ```bash
   cd "C:\Users\ALFA\OneDrive\Рабочий стол\тишина"
   ```

---

### Ошибка: "go: go.mod file not found"

**Причина:** Терминал не в корне проекта

**Решение:**
```bash
# Перейди в корень проекта
cd "C:\Users\ALFA\OneDrive\Рабочий стол\тишина"

# Проверь, что go.mod есть
dir go.mod

# Запусти сервер
go run cmd/server/main.go
```

---

## 🔍 Проверка правильности

### Шаг 1: Проверь текущую директорию
```bash
# CMD
cd
dir go.mod

# PowerShell
pwd
Test-Path go.mod
```

**Должно быть:**
- Директория: `C:\Users\ALFA\OneDrive\Рабочий стол\тишина`
- Файл `go.mod` существует

### Шаг 2: Проверь наличие файлов
```bash
# CMD
dir cmd\server\main.go

# PowerShell
Test-Path cmd\server\main.go
```

**Должно быть:** `cmd\server\main.go` существует

### Шаг 3: Запусти сервер
```bash
go run cmd/server/main.go
```

**Должно появиться:**
```
Starting dice service on :9190
```

---

## 📋 Чеклист перед запуском

- [ ] Терминал открыт в папке проекта
- [ ] Файл `go.mod` существует
- [ ] Файл `cmd/server/main.go` существует
- [ ] Go установлен (`go version` работает)
- [ ] Зависимости установлены (`go mod download`)

---

## 💡 Быстрый старт

1. **Открой папку проекта в проводнике**

2. **Правый клик → "Открыть в терминале"** (или "Open in Terminal")

3. **Выполни:**
   ```bash
   go run cmd/server/main.go
   ```

4. **Открой браузер:**
   ```
   http://localhost:9190/ui/index.html
   ```

---

## 🆘 Если ничего не помогает

1. **Скопируй полный путь к проекту:**
   ```
   C:\Users\ALFA\OneDrive\Рабочий стол\тишина
   ```

2. **Открой терминал (CMD или PowerShell)**

3. **Выполни:**
   ```bash
   cd "C:\Users\ALFA\OneDrive\Рабочий стол\тишина"
   go run cmd/server/main.go
   ```

4. **Если ошибка остаётся - скопируй полный текст ошибки**

---

## ✅ Правильный порядок действий

```
1. Открыть папку проекта
   ↓
2. Открыть терминал в этой папке
   ↓
3. Выполнить: go run cmd/server/main.go
   ↓
4. Дождаться: "Starting dice service on :9190"
   ↓
5. Открыть браузер: http://localhost:9190/ui/index.html
```

**Готово! Сервис работает!** ✅

