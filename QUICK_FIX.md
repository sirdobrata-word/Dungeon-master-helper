# 🚀 Быстрое решение проблем с сервисом в браузере

## ❌ Проблема: "Сервис не работает в браузере"

### ✅ Решение за 3 шага:

#### Шаг 1: Запусти сервер
```bash
# Открой терминал в папке проекта и выполни:
go run cmd/server/main.go
```

**Должно появиться:**
```
Starting dice service on :9190
```

#### Шаг 2: Открой правильный адрес
**✅ ПРАВИЛЬНО:**
```
http://localhost:9190/ui/index.html
```

**❌ НЕПРАВИЛЬНО:**
```
file:///C:/Users/.../index.html
```

#### Шаг 3: Проверь консоль браузера
1. Нажми **F12** (открыть DevTools)
2. Перейди на вкладку **Console**
3. Должны быть сообщения:
   - ✅ "Сервер доступен"
   - ❌ Если есть ошибки - читай дальше

---

## 🔍 Частые ошибки и решения

### Ошибка 1: "Failed to fetch"
**Причина:** Сервер не запущен

**Решение:**
```bash
go run cmd/server/main.go
```

---

### Ошибка 2: Страница открыта как `file:///...`
**Причина:** Файл открыт напрямую, а не через сервер

**Решение:**
1. Запусти сервер
2. В браузере введи: `http://localhost:9190/ui/index.html`
3. НЕ открывай файл напрямую из проводника

---

### Ошибка 3: "404 Not Found"
**Причина:** Неправильный URL или сервер не запущен

**Решение:**
1. Проверь, что сервер запущен
2. Проверь URL: должен быть `http://localhost:9190/ui/index.html`
3. Проверь в терминале, нет ли ошибок

---

### Ошибка 4: "Порт 9190 занят"
**Причина:** Другой процесс использует порт 9190

**Решение:**
```powershell
# Найди и закрой процесс
Get-NetTCPConnection -LocalPort 9190 | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force }

# Или используй другой порт
$env:PORT="3000"; go run cmd/server/main.go
```

---

## 🧪 Тест работоспособности

### Автоматический тест:
```powershell
.\test-server.ps1
```

### Ручной тест:
1. Открой: `http://localhost:9190/healthz`
2. Должно быть: `ok`

---

## 📋 Чеклист

Перед тем как сказать "не работает", проверь:

- [ ] Сервер запущен? (`go run cmd/server/main.go`)
- [ ] В терминале видно "Starting dice service on :9190"?
- [ ] Страница открыта через `http://localhost:9190/ui/index.html`?
- [ ] НЕ открыта как `file:///...`?
- [ ] В консоли браузера (F12) есть сообщения?
- [ ] На вкладке Network видны запросы?

---

## 💡 Полезные команды

### Проверить, запущен ли сервер:
```bash
curl http://localhost:9190/healthz
```

### Проверить порт:
```powershell
Get-NetTCPConnection -LocalPort 9190
```

### Остановить все процессы Go:
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*go*"} | Stop-Process -Force
```

---

## 🆘 Если ничего не помогло

1. **Перезапусти сервер:**
   ```bash
   # Останови (Ctrl+C)
   # Запусти снова
   go run cmd/server/main.go
   ```

2. **Проверь логи сервера:**
   - В терминале должны быть логи каждого запроса

3. **Проверь консоль браузера:**
   - F12 → Console
   - Скопируй все ошибки

4. **Проверь Network:**
   - F12 → Network
   - Попробуй выполнить действие
   - Посмотри, какие запросы идут и их статус

---

## ✅ Правильный порядок действий

1. **Запусти сервер:**
   ```bash
   go run cmd/server/main.go
   ```

2. **Дождись сообщения:**
   ```
   Starting dice service on :9190
   ```

3. **Открой браузер:**
   ```
   http://localhost:9190/ui/index.html
   ```

4. **Проверь консоль (F12):**
   - Должно быть: "✅ Сервер доступен"

5. **Попробуй бросить кубик:**
   - Должно работать!

---

**Если выполнил все шаги и не работает - опиши конкретную ошибку из консоли браузера!**

