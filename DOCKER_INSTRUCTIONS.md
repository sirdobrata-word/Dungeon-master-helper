# 🐳 Инструкция по работе с Docker-контейнером

## 📋 Содержание
1. [Требования](#требования)
2. [Быстрый старт](#быстрый-старт)
3. [Сборка образа](#сборка-образа)
4. [Запуск контейнера](#запуск-контейнера)
5. [Остановка и удаление](#остановка-и-удаление)
6. [Проверка работы](#проверка-работы)
7. [Использование docker-compose](#использование-docker-compose)
8. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

- **Docker Desktop** установлен и запущен
  - Скачать: https://www.docker.com/products/docker-desktop/
  - После установки убедись, что Docker запущен (иконка в трее)

---

## 🚀 Быстрый старт

### Вариант 1: Через docker-compose (рекомендуется)

```bash
# Собрать и запустить одной командой (включая PostgreSQL)
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

**Что происходит:**
- Запускается PostgreSQL контейнер
- Создаётся база данных `dnd_service`
- Запускается сервис и подключается к PostgreSQL
- Все данные (персонажи, монстры, компании) сохраняются в БД

Сервис будет доступен по адресу: **http://localhost:9190/ui/index.html**

📝 **Примечание:** Все данные теперь сохраняются в PostgreSQL и не пропадают при перезапуске!

---

## 📦 Сборка образа

### Шаг 1: Открой терминал в папке проекта

```bash
cd "C:\Users\ALFA\OneDrive\Рабочий стол\тишина"
```

### Шаг 2: Собери Docker-образ

```bash
docker build -t dnd-service .
```

**Что происходит:**
- Docker читает `Dockerfile`
- Скачивает базовый образ Go 1.21
- Устанавливает зависимости (`go mod download`)
- Компилирует приложение
- Создаёт минимальный образ с бинарником

**Время сборки:** ~2-5 минут (при первой сборке)

---

## ▶️ Запуск контейнера

### Базовый запуск

```bash
docker run -d -p 9190:9190 --name dnd-service dnd-service
```

**Параметры:**
- `-d` — запуск в фоновом режиме
- `-p 9190:9190` — проброс порта (хост:контейнер)
- `--name dnd-service` — имя контейнера
- `dnd-service` — имя образа

### Запуск с изменением порта

```bash
docker run -d -p 8080:9190 -e PORT=9190 --name dnd-service dnd-service
```

Теперь сервис будет доступен на порту **8080** хоста.

---

## ⏹️ Остановка и удаление

### Остановить контейнер

```bash
docker stop dnd-service
```

### Запустить остановленный контейнер

```bash
docker start dnd-service
```

### Удалить контейнер

```bash
docker rm dnd-service
```

### Удалить образ

```bash
docker rmi dnd-service
```

### Полная очистка (остановить + удалить)

```bash
docker stop dnd-service
docker rm dnd-service
```

---

## ✅ Проверка работы

### 1. Проверь, что контейнер запущен

```bash
docker ps
```

Должна быть строка с `dnd-service` и статусом `Up`.

### 2. Проверь логи

```bash
docker logs dnd-service
```

Должно быть:
```
Starting dice service on :9190
```

### 3. Открой в браузере

- **Главная страница:** http://localhost:9190/ui/index.html
- **Список персонажей:** http://localhost:9190/ui/list.html
- **Библиотека монстров:** http://localhost:9190/ui/monsters.html
- **Склад компаний:** http://localhost:9190/ui/companies.html
- **Health check:** http://localhost:9190/healthz

---

## 🎯 Использование docker-compose

### Запуск

```bash
# Собрать и запустить
docker-compose up --build

# Запустить в фоне
docker-compose up -d --build
```

### Остановка

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f
```

### Пересборка после изменений

```bash
docker-compose up --build
```

---

## 🔍 Полезные команды

### Просмотр всех контейнеров (включая остановленные)

```bash
docker ps -a
```

### Просмотр образов

```bash
docker images
```

### Войти внутрь контейнера (для отладки)

```bash
docker exec -it dnd-service sh
```

### Просмотр использования ресурсов

```bash
docker stats dnd-service
```

### Очистка неиспользуемых ресурсов

```bash
docker system prune -a
```

⚠️ **Внимание:** Эта команда удалит все неиспользуемые образы, контейнеры и сети!

---

## 🐛 Решение проблем

### Проблема: "port is already allocated" или "bind: Only one usage of each socket address"

**Причина:** Порт 9190 уже занят (возможно, сервер запущен через `go run` или `start-server.bat`).

**Решение 1: Освободить порт (рекомендуется)**

Запусти скрипт:
```bash
stop-server.bat
```

Или вручную:
```bash
# Найди процесс
netstat -ano | findstr :9190

# Останови процесс (замени <PID> на номер из вывода выше)
taskkill /F /PID <PID>
```

**Решение 2: Использовать другой порт**

Вариант A - через docker-compose:
```bash
docker-compose -f docker-compose-alt.yml up --build
```
Теперь сервис будет на порту **9191**: http://localhost:9191/ui/index.html

Вариант B - через docker run:
```bash
docker run -d -p 9191:9190 --name dnd-service dnd-service
```

### Проблема: "Cannot connect to the Docker daemon"

**Решение:** Docker Desktop не запущен. Запусти Docker Desktop и подожди, пока он полностью загрузится.

### Проблема: "no such file or directory" при сборке

**Решение:** Убедись, что ты находишься в корне проекта (там, где `Dockerfile` и `go.mod`).

### Проблема: Контейнер сразу останавливается

**Решение:** Проверь логи:

```bash
docker logs dnd-service
```

### Проблема: Изменения в коде не применяются

**Решение:** Пересобери образ:

```bash
docker-compose up --build
# или
docker build -t dnd-service . && docker run -d -p 9190:9190 --name dnd-service dnd-service
```

---

## 📝 Примечания

- **PostgreSQL включён:** По умолчанию `docker-compose.yml` включает PostgreSQL. Все данные сохраняются в БД и не пропадают при перезапуске.
- **Локальный запуск без Docker:** Если запускаешь через `go run`, сервис будет использовать in-memory хранилище, если не задан `DATABASE_URL`.
- **Размер образа:** Финальный образ ~20-25 МБ (благодаря многоэтапной сборке).
- **Подробнее о PostgreSQL:** См. файл `POSTGRES_SETUP.md` для детальной информации.

---

## 🎉 Готово!

Теперь твой сервис работает в Docker-контейнере! 🚀

