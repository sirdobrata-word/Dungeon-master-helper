# 🔧 Диагностика проблем с сервисом в браузере

## Частые проблемы и решения

### 1. Сервер не запущен

**Симптомы:**
- Страница загружается, но запросы не работают
- Ошибка "Failed to fetch" в консоли
- Все кнопки не работают

**Решение:**
```bash
# Запусти сервер
go run cmd/server/main.go

# Или используй скрипт
start-server.bat
```

**Проверка:**
Открой в браузере: `http://localhost:9190/healthz`
Должно быть: `ok`

---

### 2. Неправильный URL в браузере

**Симптомы:**
- Страница открыта как `file:///C:/...` вместо `http://localhost:9190/ui/...`
- Все запросы не работают

**Решение:**
✅ **Правильно:** `http://localhost:9190/ui/index.html`
❌ **Неправильно:** `file:///C:/Users/.../index.html`

**Как открыть правильно:**
1. Запусти сервер
2. Открой браузер
3. Введи адрес: `http://localhost:9190/ui/index.html`

---

### 3. Порт занят другим приложением

**Симптомы:**
- Ошибка при запуске: "bind: address already in use"
- Сервер не запускается

**Решение:**
```bash
# Вариант 1: Используй другой порт
$env:PORT="3000"; go run cmd/server/main.go

# Вариант 2: Найди и закрой процесс на порту 9190
# Windows PowerShell
Get-NetTCPConnection -LocalPort 9190 | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force }
```

---

### 4. Проблемы с путями к API

**Симптомы:**
- Ошибка 404 при запросах
- Запросы идут не туда

**Проверка:**
1. Открой консоль браузера (F12)
2. Посмотри на вкладку Network
3. Проверь, куда идут запросы

**Правильные пути:**
- `http://localhost:9190/roll`
- `http://localhost:9190/characters`
- `http://localhost:9190/monsters`
- `http://localhost:9190/companies`

---

### 5. Проблемы с CORS (если открываешь с другого домена)

**Симптомы:**
- Ошибка "CORS policy" в консоли
- Запросы блокируются

**Решение:**
Сервер уже настроен для работы с localhost. Если нужен другой домен, добавь CORS middleware.

---

## 🔍 Пошаговая диагностика

### Шаг 1: Проверь, запущен ли сервер

```bash
# В терминале выполни:
curl http://localhost:9190/healthz

# Должно вернуть: ok
```

### Шаг 2: Проверь в браузере

1. Открой: `http://localhost:9190/healthz`
2. Должно быть: `ok`

### Шаг 3: Проверь консоль браузера

1. Открой страницу: `http://localhost:9190/ui/index.html`
2. Нажми F12 (открыть DevTools)
3. Перейди на вкладку Console
4. Посмотри на ошибки (красные сообщения)

### Шаг 4: Проверь Network запросы

1. В DevTools перейди на вкладку Network
2. Попробуй выполнить действие (например, бросить кубик)
3. Посмотри на запросы:
   - Должны быть запросы к `/roll`, `/characters` и т.д.
   - Статус должен быть 200 (OK) или другой успешный

---

## 🛠️ Быстрое решение

### Если ничего не работает:

1. **Останови все процессы Go:**
   ```powershell
   Get-Process | Where-Object {$_.ProcessName -like "*go*"} | Stop-Process -Force
   ```

2. **Перезапусти сервер:**
   ```bash
   go run cmd/server/main.go
   ```

3. **Открой в браузере:**
   ```
   http://localhost:9190/ui/index.html
   ```

4. **Проверь консоль браузера (F12):**
   - Должны быть логи запросов
   - Не должно быть ошибок

---

## 📝 Чеклист проверки

- [ ] Сервер запущен (`go run cmd/server/main.go`)
- [ ] Сервер отвечает на `/healthz` (возвращает `ok`)
- [ ] Страница открыта через `http://localhost:9190/ui/...`
- [ ] НЕ открыта как `file:///...`
- [ ] Порт 9190 свободен
- [ ] В консоли браузера нет ошибок
- [ ] В Network видны запросы к API

---

## 💡 Полезные команды

### Проверка порта
```powershell
# Проверить, занят ли порт 9190
Get-NetTCPConnection -LocalPort 9190
```

### Остановка процесса на порту
```powershell
# Найти и остановить процесс на порту 9190
$process = Get-NetTCPConnection -LocalPort 9190 | Select-Object -ExpandProperty OwningProcess
Stop-Process -Id $process -Force
```

### Проверка Go
```bash
go version
```

### Проверка компиляции
```bash
go build ./cmd/server
```

---

## 🆘 Если ничего не помогло

1. **Создай issue с описанием:**
   - Какая ошибка в консоли браузера?
   - Что показывает Network?
   - Какой URL в адресной строке?
   - Запущен ли сервер?

2. **Проверь логи сервера:**
   - В терминале, где запущен сервер, должны быть логи запросов

3. **Попробуй пересобрать:**
   ```bash
   go clean -cache
   go mod download
   go build ./cmd/server
   ./server.exe
   ```

